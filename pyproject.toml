[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dataforge"
version = "0.0.0"
description = "DataForge project"
readme = "README.md"
authors = [ { name = "Your Name" } ]
requires-python = ">=3.8"
